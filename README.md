Create Impulse Tracker .it files from a JSON song datastructure. **WIP**

`npm run example`

Usage: `const buffer = itwriter(song_structure)`

See [./example.js](example.js) for details of the expected JSON song structure.
